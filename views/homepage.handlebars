{{#each blogs as |blog|}}
<div>
    <div>
        <h2>
            <a href="/blog/{{blog.id}}">{{blog.title}}</a>
        </h2>
        <p>
            {{blog.content}}
        </p>
        <p>Written By: {{user.name}}</p>
    </div>
</div>
{{/each}}